# Edge Detection Benchmark — Vector Lighting и цветовые baseline'ы

[![Python](https://img.shields.io/badge/python-3.10+-blue.svg)](https://www.python.org/)
[![License](https://img.shields.io/badge/license-MIT-green.svg)](LICENSE)
[![Version](https://img.shields.io/badge/version-1.0.0-orange.svg)]()
[![DOI](https://zenodo.org/badge/1201389926.svg)](https://doi.org/10.5281/zenodo.19420796)
[![en](https://img.shields.io/badge/lang-en-blue.svg)](README.md)

Небольшой воспроизводимый бенчмарк выделения границ. Реализует классические полутоновые операторы (Sobel, Prewitt, Canny), цветовые операторы (структурный тензор Ди Дзензо и варианты) и `vector_lighting` — метод, выведенный здесь из модели «виртуального освещения» RGB-каналов.

> ### ⚠️ Честный статус метода `vector_lighting`
> Проект начинался как попытка оригинального цветового детектора границ. Строгое сравнение с правильным **цветовым** эталоном — **структурным тензором Ди Дзензо (1986)** — показало:
> - **Ядро `vector_lighting` математически эквивалентно цветовому структурному тензору Ди Дзензо.** С отключённым height-членом его карта отклика ранг-идентична Di Zenzo (Spearman ρ ≈ 0.99 на реальных изображениях, 1.00 на изолюминантных тестах). Конструкция «виртуальное освещение + перестановки каналов» — это **альтернативный вывод уже существующего оператора**.
> - Единственная отличительная часть — асимметричный по каналам *height*-член — возмущает результат лишь на ~2–3% и **не улучшает** точность на обычных RGB (на BSDS500 слегка ухудшает).
> - Преимущество «цветочувствительности» над полутоновым Canny **реально, но не уникально** — у Di Zenzo оно тоже есть.
>
> Таким образом, `vector_lighting` — **не** превосходящий новый детектор. Этот репозиторий правильнее понимать как **бенчмарк и независимый повторный вывод**, а не как заявление о новизне. См. [`docs/benchmark_report_2026-06.md`](docs/benchmark_report_2026-06.md); эквивалентность воспроизводится командой `python tests/correspondence_analysis.py`.

## Автор

**Панченко Александр Алексеевич** — радиофизик, студент  
[GitHub](https://github.com/Nervni-Sanya)

## Что внутри

- **Полутоновые детекторы:** Sobel, Prewitt, Canny (реализации на NumPy/SciPy).
- **Цветовые детекторы:** структурный тензор Ди Дзензо, цветной Canny на его основе, multichannel-Sobel и `vector_lighting`.
- **Воспроизводимый харнесс** (`tests/testing.py`): синтетика, тест ориентации/анизотропии, тест масштабирования, оценка на реальных изображениях BSDS500.
- **Анализ соответствия** (`tests/correspondence_analysis.py`), количественно показывающий эквивалентность `vector_lighting` ↔ Di Zenzo.

Все числа ниже воспроизводимы. Абсолютное время зависит от машины.

## 📊 Результаты бенчмарка

### Синтетика (F1, допуск 2 px, 256×256)

`python tests/testing.py --baseline-only`. Sobel/Prewitt/Di Zenzo бинаризуются по 95-му перцентилю; F1 на отдельных синтетических изображениях чувствителен к этому выбору.

| Изображение | Sobel | Canny | DiZenzo | VectorLight |
|-------------|:-----:|:-----:|:-------:|:-----------:|
| `checkerboard` | 0.000 | 0.945 | 0.739 | 0.933 |
| `concentric_circles` | 1.000 | 1.000 | 1.000 | 1.000 |
| `color_patches_equal_brightness` | 1.000 | 0.000 | 0.909 | 0.909 |
| `color_wheel` | 0.510 | 0.471 | 0.340 | 0.887 |
| `blurred_disk` | 1.000 | 1.000 | 1.000 | 1.000 |
| `gray_scale` | 1.000 | 1.000 | 0.000 | 0.000 |
| **Средний F1** | 0.752 | 0.736 | 0.665 | 0.788 |

На этой маленькой синтетике `vector_lighting` и Di Zenzo выигрывают друг у друга по разным изображениям (это один и тот же оператор, поэтому разброс по изображениям — следствие хрупкости порога, а не разницы в качестве). Решающее сравнение — на реальных изображениях ниже.

### Цветочувствительность (изолюминантные границы) — преимущество общее

Полосы из цветов равной яркости, 12 ориентаций (`python tests/testing.py --orientation`):

| Метод | F1 на изолюминантных границах |
|-------|:----------------------------:|
| VectorLight | ~0.99 |
| **DiZenzo** | **~0.99** |
| Canny | 0.000 |

Оба цветовых метода видят изолюминантные границы; полутоновый Canny — нет. Это исправленная версия прежнего заявления «уникально для нашего метода» — оно **не** уникально.

### Реальные изображения — BSDS500 (test-сплит, 200 изобр.)

`python tests/testing.py --bsds /путь/к/BSDS500`. GT — объединение аннотаторов, допуск 0.0075×диагональ, фиксированные пороги.

| Метод | F1 | Precision | Recall |
|-------|:--:|:---------:|:------:|
| **DiZenzo** | **0.565** | **0.637** | 0.526 |
| Canny | 0.566 | 0.482 | 0.774 |
| VL-default | 0.552 | 0.605 | 0.504 |
| Prewitt | 0.543 | 0.582 | 0.529 |
| Sobel | 0.541 | 0.577 | 0.530 |

На реальных фото Di Zenzo слегка опережает `vector_lighting` (0.565 против 0.552). Все рукотворные детекторы существенно ниже современных обучаемых методов (BSDS ODS ≈ 0.83–0.84).

### Эквивалентность методов (`tests/correspondence_analysis.py`)

| Сравнение | Spearman ρ (структурные изобр.) | Смысл |
|---|:--:|---|
| VectorLight(α=0) ↔ DiZenzo | **0.99–1.00** | Ранг-идентичны ⇒ тот же оператор |
| height-член (α=1 vs α=0) | ~0.98 | Вносит лишь ~2–3% возмущения |

## Использование как библиотеки

```python
from vector_lighting import sobel, prewitt, canny, vector_lighting, EdgeDetector

edges = vector_lighting(image)            # цветовая карта границ (uint8)
edges = canny(image, low_threshold=50, high_threshold=100)
```

Детекторы пригодны как небольшая библиотека; параметры `vector_lighting` описаны в [исходнике](vector_lighting/core.py). Для стандартного, хорошо изученного цветового детектора предпочтительнее структурный тензор Ди Дзензо (`tests/color_baselines.py`).

## Воспроизведение

```bash
pip install numpy scipy pandas pillow tqdm matplotlib openpyxl

python tests/testing.py --baseline-only         # синтетика + замер скорости
python tests/testing.py --orientation           # анизотропия / диагонали
python tests/testing.py --scaling               # поведение по размеру
git clone --depth 1 https://github.com/BIDS/BSDS500 /путь/к/BSDS500
python tests/testing.py --bsds /путь/к/BSDS500   # реальные изображения
python tests/correspondence_analysis.py --bsds /путь/к/BSDS500  # VL против Di Zenzo
```

## Литература

- S. Di Zenzo, "A note on the gradient of a multi-image", *CVGIP* 33(1):116–125, 1986.
- J. Canny, "A computational approach to edge detection", *IEEE TPAMI* 8(6):679–698, 1986.

## Благодарности

Инструменты генеративного ИИ использовались для рефакторинга кода, бенчмаркинга и подготовки документации.

```bash
pip install git+https://github.com/Nervni-Sanya/Edge-detection-benchmark.git
```
